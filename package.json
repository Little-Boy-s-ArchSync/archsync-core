{
  "name": "@archsync/core",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "description": "Architecture model, validation and graph primitives for ArchSync.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Little-Boy-s-ArchSync/archsync-core.git"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "dist",
    "specs",
    "README.md"
  ],
  "bin": {
    "archsync": "dist/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false && tsc -p tsconfig.test.json --noEmit --pretty false",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "cli:smoke": "node scripts/cli-smoke.mjs",
    "evidence:update": "pnpm test:coverage && pnpm build && node scripts/phase1-evidence.mjs --write",
    "evidence:verify": "node scripts/phase1-evidence.mjs",
    "demo:reports": "tsx src/bin.ts report test/fixtures/order-platform.architecture.yaml test/fixtures/order-platform.violation.architecture.yaml docs/demo/order-platform-violation-report.mmd && tsx src/bin.ts report test/fixtures/order-platform.architecture.yaml test/fixtures/order-platform.violation.architecture.yaml docs/demo/order-platform-violation-report.drawio && tsx src/bin.ts report test/fixtures/order-platform.architecture.yaml test/fixtures/order-platform.evolution.architecture.yaml docs/demo/order-platform-evolution-report.mmd && tsx src/bin.ts report test/fixtures/order-platform.architecture.yaml test/fixtures/order-platform.evolution.architecture.yaml docs/demo/order-platform-evolution-report.drawio",
    "arch:model": "tsx src/bin.ts",
    "validate:fixtures": "tsx src/bin.ts validate-dir test/fixtures",
    "phase1:verify": "pnpm typecheck && pnpm test:coverage && pnpm validate:fixtures && pnpm build && pnpm cli:smoke && pnpm evidence:verify"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "3.2.7",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
